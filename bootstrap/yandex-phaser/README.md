# __PROJECT_NAME__

Generated from `DanilaH/mini-games-kit` mandatory Yandex + Phaser bootstrap at commit `__MINI_GAMES_KIT_REF__`.

Before feature work, read `AGENTS.md`, `BOOTSTRAP.md` and the docs in `docs/`, then inspect the current `mini-games-kit/docs/API.md` for additional reusable primitives that fit this game.

## Commands

```bash
npm install
npm run dev
npm run typecheck
npm test
npm run build
npm run build:yandex:debug
npm run release:check
```

`npm run build` is the production Yandex build. `build:yandex:debug` keeps the real Yandex runtime but enables the bootstrap debug panel for hosted DRAFT diagnostics; it is not the submission candidate.
